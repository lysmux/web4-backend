<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="004-create-password-credentials-table" author="lysmux">
        <createTable tableName="password_credentials">

            <column name="user_id" type="UUID">
                <constraints nullable="false" primaryKey="true"
                             foreignKeyName="fx_password_cred_user_id"
                             references="users(id) ON DELETE CASCADE"/>
            </column>

            <column name="password" type="VARCHAR(60)">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="updated_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

        </createTable>

        <createIndex tableName="password_credentials" indexName="idx_password_cred_user_id">
            <column name="user_id" />
        </createIndex>

        <sql>
            CREATE TRIGGER update_password_cred_updated_at
                BEFORE UPDATE ON password_credentials
                FOR EACH ROW
                EXECUTE FUNCTION update_updated_at_column();
        </sql>

    </changeSet>

</databaseChangeLog>